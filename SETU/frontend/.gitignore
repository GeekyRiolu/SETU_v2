# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
*.tsbuildinfo
next-env.d.ts

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env
.env*.local

# The repo-root .gitignore has `lib/` (Python build dir); it also matches this
# frontend's source lib/. Re-include it here so app code is actually tracked.
!/lib/
!/lib/**
